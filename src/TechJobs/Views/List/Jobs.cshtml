<h1>@ViewBag.title</h1>

@if (ViewBag.jobs != null)
{
    <h3>@ViewBag.jobs.Count Result(s)</h3>

    @foreach (Dictionary<string, string> job in ViewBag.jobs)
    {
        <table class="job-listing">
            @foreach (KeyValuePair<string, string> jobList in job)
            {
                <tr>
                    <td>@jobList.Key</td>
                    <td>@jobList.Value</td>
                </tr>

            }
        </table>
    }
}